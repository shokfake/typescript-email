import http from '@/plugins/http';

export default class UserAPI {

    static isNeedEncryption() {
        // const serverConfig = getApp().globalData.defaultConfig
        return http.post('')
    }
}